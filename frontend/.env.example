# Data source mode: "mock" runs entirely on seeded demo data (no backend needed).
# "live" talks to your real LifeCode API using the documented endpoints.
VITE_API_MODE=mock

# Base URL of your LifeCode backend (used only when VITE_API_MODE=live).
# Matches the documented base: <host>/api/app
VITE_API_BASE_URL=https://life-code--yossfabdla311.replit.app/api/app
